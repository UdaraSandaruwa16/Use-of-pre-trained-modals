# Use-of-pre-trained-modals
In this repository you can get to know the use of few of pre-trained modals in Hugginface. Refer the README file.

## Install before run
  ###pip install pyttsx3
  ###pip install PyAudio
  ###pip install SpeechRecognition
